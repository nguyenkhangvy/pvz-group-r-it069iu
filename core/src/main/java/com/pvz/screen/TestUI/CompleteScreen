package com.pvz.screen;

import com.badlogic.gdx.Gdx;
import com.badlogic.gdx.graphics.Color;
import com.badlogic.gdx.graphics.g2d.BitmapFont;
import com.badlogic.gdx.graphics.g2d.GlyphLayout;
import com.badlogic.gdx.scenes.scene2d.Actor;
import com.badlogic.gdx.scenes.scene2d.Stage;
import com.badlogic.gdx.scenes.scene2d.ui.*;
import com.badlogic.gdx.scenes.scene2d.utils.ChangeListener;
import com.pvz.core.GameConfig;
import com.pvz.data.LevelData;
import com.pvz.manager.DataManager;
import com.pvz.manager.SaveManager;
import com.pvz.manager.ScreenManager;
import com.pvz.util.DebugDraw;


public class CompleteScreen extends BaseScreen {

    private final Stage stage;
    private static final Color BG   = new Color(0.08f, 0.20f, 0.04f, 1f);
    private static final Color GOLD = new Color(1.00f, 0.85f, 0.10f, 1f);
    private BitmapFont bigFont;
    private final GlyphLayout layout = new GlyphLayout();

    CompleteScreen() {
        stage = new Stage(viewport, batch);
        Gdx.input.setInputProcessor(stage);
        bigFont = new BitmapFont();
        bigFont.getData().setScale(2.2f);
        build();
    }

    private void build() {
        Table root = new Table();
        root.setFillParent(true);
        root.center();
        root.add().height(180).row();

        Label sub1 = new Label("You completed all levels!", UiAssets.skin(), "subtitle");
        Label sub2 = new Label("Coming soon: more plants, zombies & night levels!", UiAssets.skin(), "subtitle");
        root.add(sub1).padBottom(10).row();
        root.add(sub2).padBottom(30).row();

        TextButton back = new TextButton("Back to Menu", UiAssets.skin(), "sun");
        back.addListener(new ChangeListener() {
            @Override public void changed(ChangeEvent e, Actor a) {
                ScreenManager.get().setScreen(new StartupScreen());
            }
        });
        root.add(back).width(260).height(65).row();
        stage.addActor(root);
    }

    @Override protected void update(float delta) { stage.act(delta); }

    @Override
    protected void draw() {
        DebugDraw dd = DebugDraw.get();
        batch.begin();
        dd.rect(batch, 0, 0, GameConfig.WORLD_WIDTH, GameConfig.WORLD_HEIGHT, BG);
        bigFont.setColor(GOLD);
        layout.setText(bigFont, "CONGRATULATIONS!");
        bigFont.draw(batch, "CONGRATULATIONS!",
            (GameConfig.WORLD_WIDTH - layout.width) / 2f,
            GameConfig.WORLD_HEIGHT - 60f);
        batch.end();
        stage.draw();
    }

    @Override public void dispose() {
        super.dispose();
        stage.dispose();
        if (bigFont != null) bigFont.dispose();
    }
}
